apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: glpi
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/richpea1982/k3s
    targetRevision: HEAD
    path: gitops/manifests/glpi
  destination:
    server: https://kubernetes.default.svc
    namespace: glpi
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
